#!/usr/bin/env bash
set -eu

root=${0%/tests/run}
if [ -z "$root" ] || [ "$root" = "$0" ]; then
  root=.
fi

node --test "$root/tests/engine.test.mjs" "$root/tests/logical-invariant.test.mjs" "$root/tests/paywall.test.mjs" "$root/tests/static-check.mjs" "$root/tests/lean-tdd.test.mjs"
